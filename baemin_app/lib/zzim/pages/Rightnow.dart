import 'package:flutter/material.dart';

class Rightnow extends StatelessWidget {
  const Rightnow({Key key,}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Color(0x09000000),
      child: ListView(),
    );
  }
}